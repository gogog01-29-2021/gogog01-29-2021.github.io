---
layout: page
title: ICML 2026 index
permalink: /icml2026/
nav: false
description: A classified index map of ICML 2026 — talks and papers with links to code and keywords. Deep write-ups are added over time and linked from here.
---

The **index map** for ICML 2026 (Seoul · COEX · July 6–11). Each row links out to
arXiv / code and carries keywords + a topic. Deep reading notes are written for
selected entries and linked in the **Post** column; the running day-by-day archive
lives in the [ICML 2026 — live archive]({{ '/blog/2026/icml-2026/' | relative_url }}) post.

## Invited talks

| Speaker | Affiliation | Talk | Topic |
|---|---|---|---|
{% for t in site.data.icml2026.invited_talks -%}
| {{ t.speaker }} | {{ t.affiliation }} | {% if t.title %}{{ t.title }}{% if t.when %} <br><small>{{ t.when }}</small>{% endif %}{% else %}<em>TBA</em>{% endif %} | `{{ t.topic }}` |
{% endfor %}

## Papers

{% assign groups = site.data.icml2026.papers | group_by: "topic" -%}
{% for g in groups %}
### {{ g.name }}

| Paper | Authors | Links | Keywords | Post |
|---|---|---|---|---|
{% for p in g.items -%}
| {{ p.title }} | {{ p.authors }} | {% if p.arxiv %}[arXiv](https://arxiv.org/abs/{{ p.arxiv }}){% endif %}{% if p.code %} · [code]({{ p.code }}){% endif %} | {% if p.keywords %}{{ p.keywords | join: " · " }}{% endif %} | {% if p.post %}[note]({{ p.post | relative_url }}){% else %}—{% endif %} |
{% endfor %}
{% endfor %}

---

**How this grows.** New entries are appended to `_data/icml2026.yml` (the index),
classified by `topic`. The full accepted-paper list is pulled in batches from
PMLR / OpenReview. A selected entry graduates into a full reading note (the
`post:` link) — the index stays the map; the posts are the territory.
